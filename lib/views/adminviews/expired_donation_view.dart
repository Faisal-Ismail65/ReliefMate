import 'package:flutter/material.dart';

class ExpiredDonationView extends StatelessWidget {
  const ExpiredDonationView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
